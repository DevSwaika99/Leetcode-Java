class Solution {
    public boolean isPalindrome(int x) {
        int d = x;
        int sum = 0;
        while (d>0) {
            sum = (10*sum)+(d%10);
            d = d/10;
        }
        if (sum == x) {
            return true;
        } else {
            return false;
        }
    }
}
